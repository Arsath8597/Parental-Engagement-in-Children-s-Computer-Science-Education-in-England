import React from "react";
import SideBar from "../Student/sidebar";

const Landing = () => {
  return (
    <div className="min-h-screen w-full bg-gradient-to-br from-sky-600 via-blue-900 to-indigo-900">
      <SideBar />
    </div>
  );
};

export default Landing;
